stages:
    - deploy

# ---

deploy:image:
    before_script:
        - 'apk add --no-cache --no-progress ca-certificates curl'
    image:
        entrypoint: ['']
        name: alpine:latest
    only:
        - master
    script:
        - 'curl --request POST "https://cloud.docker.com/api/build/v1/source/2436146a-f293-4189-80ff-3fae19dddb9d/trigger/${DOCKER_CLOUD_TOKEN}/call/"'
    stage: deploy
